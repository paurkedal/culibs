PACKAGE_VERSION=@PACKAGE_VERSION@
prefix=@prefix@
exec_prefix=@exec_prefix@
top_builddir=@abs_top_builddir@
top_srcdir=@abs_top_srcdir@

Name: custo
Description: Collected C: Serialisation, Transport, and Storage (custo)
Version: ${PACKAGE_VERSION}
Requires: cubase
Libs: ${top_builddir}/libcusto.la
Cflags: -I${top_builddir} -I${top_srcdir}
