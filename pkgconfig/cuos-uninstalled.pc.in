PACKAGE_VERSION=@PACKAGE_VERSION@
prefix=@prefix@
exec_prefix=@exec_prefix@
top_builddir=@abs_top_builddir@
top_srcdir=@abs_top_srcdir@

Name: cuos
Description: Collected C: Serialisation, Transport, and Storage (cuos)
Version: ${PACKAGE_VERSION}
Requires: cubase
Libs: ${top_builddir}/libcuos.la
Cflags: -I${top_builddir} -I${top_srcdir}
